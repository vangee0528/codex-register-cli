# CLI environment example
# Copy this file to .env only if you need to override runtime paths.

# Override the config file path.
# APP_CONFIG_PATH=./config.json

# Override the database path or URL.
# APP_DATABASE_URL=data/database.db
# DATABASE_URL=sqlite:///data/database.db
